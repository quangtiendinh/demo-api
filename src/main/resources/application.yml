spring:
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        show_sql: true
    database: postgresql
  redis:
    host: localhost
    port: 6379
    timeout: 10000ms
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres-db:5432/demo-db
    username: postgres
    password: 12345
server:
  port: 9090